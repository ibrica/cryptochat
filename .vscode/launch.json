{
    "version": "0.2.0",
    "configurations": [
      {
        "type": "node",
        "request": "launch",
        "name": "Debug TS",
       // "preLaunchTask": "typescript",
        "program": "${workspaceFolder}/server/src/server.ts",
        "cwd": "${workspaceFolder}",
        "protocol": "inspector",
        "outFiles": [
          "${workspaceFolder}/server/dist/**/*.js"
        ]
      }
    ]
  }